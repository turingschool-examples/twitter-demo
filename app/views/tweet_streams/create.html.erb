<div>
  <ul>
    <% @tweets.each do |tweet| %>
      <li class="tweet">
        <p><em><%= tweet.user.screen_name %></em> says: <%= tweet.message %></p>
      </li>
    <% end %>
  </ul>
</div>

<p>
  <%= link_to("try another user", new_tweet_stream_path) %>
</p>
