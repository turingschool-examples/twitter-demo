<h3>Here's some recents tweets for <%= @user_name %>:</h3>
<h5>(<%= @user_name %> has <%= @follower_count %> followers)</h5>
<div>
  <ul>
    <% @tweets.each do |tweet| %>
      <li class="tweet">
      <p>
      <em>
        <%= tweet.user.screen_name %>
      </em>
        says: <%= tweet.text %>
      </p>
      </li>
    <% end %>
  </ul>
</div>

<p>
  <%= link_to("try another user", new_tweet_stream_path) %>
</p>
